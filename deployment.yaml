apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: d-web
spec:
  replicas: 5
  template:
    metadata:
      labels:
        web: rails
    spec:
      containers:
      - name: web
        image: popcorn:1
        ports:
        - containerPort: 3333